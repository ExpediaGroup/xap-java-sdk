rule:
  kind: user_type
  inside:
    any:
      - kind: parameter
        regex: pickupTime
      - kind: parameter
        regex: dropOffTime
    stopBy:
      kind: class_declaration
      regex: Builder
